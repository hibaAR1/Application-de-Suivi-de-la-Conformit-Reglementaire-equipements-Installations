# Mon App (React + Express)

Instructions rapides:

1. Installer les dépendances root (concurrently) et celles des sous-projets:

```bash
npm install
npm run install-all
```

2. Lancer le front et le backend en mode développement:

```bash
npm run dev
```

Front: http://localhost:5173
Backend: http://localhost:5000
